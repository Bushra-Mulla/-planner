import React, { Component } from "react";

export default class NewProject extends Component {
  render() {
    return (
      <div>
        {/* post new calendar and save calendar id + make it public // project =>
        dashbourd (alltask)=>event (calendar and timeline) //calendarlist */}
      </div>
    );
  }
}
